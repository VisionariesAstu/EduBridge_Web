import { Blinker, Fugaz_One, Inter, Roboto_Mono } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const fugaz_one = Fugaz_One({
  weight: ["400"],
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
});

export const roboto_mono = Roboto_Mono({
  weight: ["400"],
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
});

export const blinker = Blinker({
  weight: ["400"],
  style: ["normal"],
  subsets: ["latin"],
  display: "swap",
});
